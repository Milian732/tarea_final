pipeline {
    agent {
        node {
            label 'local'
        }
    }
    stages {
        stage('Conectar local') {
            steps {
                sh 'echo hola';
            }
        }
    }
}
